{
  "name": "dictionary-autocomplete",
  "displayName": "Dictionary Autocomplete",
  "description": "Inline word completions from English/German dictionaries with frequency ranking, fuzzy matching, phrase completion, and n-gram predictions",
  "version": "0.3.0",
  "publisher": "steimbyte",
  "license": "MIT",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/steimbyte/dictionary-autocomplete.git"
  },
  "bugs": {
    "url": "https://github.com/steimbyte/dictionary-autocomplete/issues"
  },
  "homepage": "https://github.com/steimbyte/dictionary-autocomplete",
  "contributes": {
    "configuration": {
      "title": "Dictionary Autocomplete",
      "type": "object",
      "properties": {
        "dictionaryAutocomplete.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable/disable dictionary autocomplete"
        },
        "dictionaryAutocomplete.languages": {
          "type": "array",
          "default": [
            "en",
            "de"
          ],
          "description": "Which dictionary languages to use (en, de, or both)"
        },
        "dictionaryAutocomplete.customPaths": {
          "type": "array",
          "default": [],
          "description": "Additional JSON dictionary files to load (overrides defaults when set)"
        },
        "dictionaryAutocomplete.minPrefixLength": {
          "type": "number",
          "default": 2,
          "minimum": 1,
          "maximum": 10,
          "description": "Minimum characters before suggestions appear"
        },
        "dictionaryAutocomplete.maxSuggestions": {
          "type": "number",
          "default": 20,
          "minimum": 5,
          "maximum": 100,
          "description": "Maximum number of suggestions in dropdown"
        },
        "dictionaryAutocomplete.fileTypes": {
          "type": "array",
          "default": [
            "*"
          ],
          "description": "File types where suggestions appear"
        },
        "dictionaryAutocomplete.fuzzyEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable fuzzy matching for typos (e.g. 'Schmetterlin' → 'Schmetterling')"
        },
        "dictionaryAutocomplete.phrasesEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable phrase completions (e.g. 'Auf W' → 'Auf Wiedersehen')"
        },
        "dictionaryAutocomplete.ngramsEnabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable n-gram next-word predictions"
        },
        "dictionaryAutocomplete.userLearning": {
          "type": "boolean",
          "default": true,
          "description": "Track typed words to boost their frequency in rankings"
        }
      }
    },
    "commands": [
      {
        "command": "dictionaryAutocomplete.reload",
        "title": "Dictionary: Reload dictionaries"
      },
      {
        "command": "dictionaryAutocomplete.addWord",
        "title": "Dictionary: Add current word"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "npm run build-dicts && npm run compile",
    "build-dicts": "node scripts/build-dicts.js",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0",
    "@vscode/vsce": "^3.0.0"
  }
}
